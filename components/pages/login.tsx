"use client";import { useState } from "react";import { useRouter } from "next/navigation";import { Building2,LogIn } from "lucide-react";
export function LoginPage(){const router=useRouter(),[email,setEmail]=useState("");return <main className="login-page"><section className="login-panel"><div className="login-brand"><Building2/><div><h1>賃貸管理</h1><p>物件・契約・入金をひとつに</p></div></div><form onSubmit={e=>{e.preventDefault();router.push("/")}}><label>メールアドレス<input type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="owner@example.com"/></label><label>パスワード<input type="password" required minLength={6}/></label><button className="primary"><LogIn/>ログイン</button></form><div className="demo-box"><b>デモモード</b><p>Supabase未設定のため、入力内容はこのブラウザに保存されます。</p></div></section></main>}
